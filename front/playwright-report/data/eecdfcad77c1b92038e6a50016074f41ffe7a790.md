# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: routing.spec.ts >> Авторизационный роутинг >> авторизованный пользователь перенаправляется на главную со страницы регистрации
- Location: test/e2e/routing.spec.ts:46:3

# Error details

```
Error: expect(page).toHaveURL(expected) failed

Expected pattern: /\/$/
Received string:  "http://127.0.0.1:5173/login"
Timeout: 10000ms

Call log:
  - Expect "toHaveURL" with timeout 10000ms
    14 × unexpected value "http://127.0.0.1:5173/login"

```

# Page snapshot

```yaml
- generic [ref=e2]:
    - generic [ref=e5]:
        - generic [ref=e6]:
            - heading "Вход в аккаунт" [level=2] [ref=e7]
            - generic [ref=e8]: Некорректный логин или пароль
            - generic [ref=e9]:
                - generic [ref=e10]: Логин, email или телефон
                - textbox "Логин, email или телефон" [ref=e11]:
                    - /placeholder: Введите логин, email или телефон
                    - text: user_1776964426127
            - generic [ref=e12]:
                - generic [ref=e13]: Пароль
                - textbox "Пароль" [ref=e14]:
                    - /placeholder: ••••••••
                    - text: password123
            - button "Войти" [ref=e15] [cursor=pointer]
        - generic [ref=e16]:
            - text: Нет аккаунта?
            - link "Зарегистрироваться" [ref=e17] [cursor=pointer]:
                - /url: /register
    - generic [ref=e18]:
        - img [ref=e20]
        - button "Open Tanstack query devtools" [ref=e69] [cursor=pointer]:
            - img [ref=e70]
```

# Test source

```ts
  1  | import { test, expect } from '@playwright/test';
  2  |
  3  | test.describe('Авторизационный роутинг', () => {
  4  |   test.beforeEach(async ({ page }) => {
  5  |     // eslint-disable-next-line no-console
  6  |     page.on('console', (msg) => console.log('BROWSER LOG:', msg.text()));
  7  |   });
  8  |
  9  |   test('неавторизованный пользователь перенаправляется на /login с главной страницы', async ({ page }) => {
  10 |     // Переходим на главную без кук
  11 |     await page.goto('/');
  12 |
  13 |     // Должны оказаться на /login
  14 |     await expect(page).toHaveURL(/\/login/);
  15 |   });
  16 |
  17 |   test('авторизованный пользователь перенаправляется на главную со страницы входа', async ({ page, context }) => {
  18 |     // Имитируем успешную регистрацию и логин
  19 |     const username = `user_${Date.now()}`;
  20 |     const email = `${username}@test.com`;
  21 |     const phone = `+7${Math.floor(1000000000 + Math.random() * 9000000000)}`;
  22 |
  23 |     await page.goto('/register');
  24 |     await page.getByLabel(/Имя пользователя/i).fill(username);
  25 |     await page.getByLabel(/Email/i).fill(email);
  26 |     await page.getByLabel(/Телефон/i).fill(phone);
  27 |     await page.getByLabel(/Пароль/i).fill('password123');
  28 |     await page.getByRole('button', { name: /Создать аккаунт/i }).click();
  29 |
  30 |     await expect(page).toHaveURL(/\/login/);
  31 |
  32 |     await page.getByLabel(/Логин, email или телефон/i).fill(username);
  33 |     await page.getByLabel(/Пароль/i).fill('password123');
  34 |     await page.getByRole('button', { name: /Войти/i }).click();
  35 |
  36 |     // Ждем редиректа на главную после логина
  37 |     await expect(page).toHaveURL(/\/$/);
  38 |
  39 |     // Пытаемся зайти на /login будучи залогиненным
  40 |     await page.goto('/login');
  41 |
  42 |     // Должны быть отброшены назад на главную
  43 |     await expect(page).toHaveURL(/\/$/);
  44 |   });
  45 |
  46 |   test('авторизованный пользователь перенаправляется на главную со страницы регистрации', async ({ page }) => {
  47 |     // Сначала логинимся (используем уже созданного юзера или регистрируем нового)
  48 |     const username = `user_${Date.now()}`;
  49 |     const phone = `+7${Math.floor(1000000000 + Math.random() * 9000000000)}`;
  50 |     await page.goto('/register');
  51 |     await page.getByLabel(/Имя пользователя/i).fill(username);
  52 |     await page.getByLabel(/Email/i).fill(`${username}@test.com`);
  53 |     await page.getByLabel(/Телефон/i).fill(phone);
  54 |     await page.getByLabel(/Пароль/i).fill('password123');
  55 |     await page.getByRole('button', { name: /Создать аккаунт/i }).click();
  56 |
  57 |     await page.goto('/login');
  58 |     await page.getByLabel(/Логин, email или телефон/i).fill(username);
  59 |     await page.getByLabel(/Пароль/i).fill('password123');
  60 |     await page.getByRole('button', { name: /Войти/i }).click();
> 61 |     await expect(page).toHaveURL(/\/$/);
     |                        ^ Error: expect(page).toHaveURL(expected) failed
  62 |
  63 |     // Пытаемся зайти на /register
  64 |     await page.goto('/register');
  65 |
  66 |     // Должны быть отброшены на главную
  67 |     await expect(page).toHaveURL(/\/$/);
  68 |   });
  69 | });
  70 |
```
