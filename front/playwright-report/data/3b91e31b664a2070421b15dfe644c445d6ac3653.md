# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: registration.spec.ts >> Регистрация >> успешная регистрация нового пользователя
- Location: test/e2e/registration.spec.ts:4:3

# Error details

```
Test timeout of 30000ms exceeded.
```

```
Error: locator.fill: Test timeout of 30000ms exceeded.
Call log:
  - waiting for getByLabel(/Имя пользователя/i)

```

# Page snapshot

```yaml
- generic [ref=e2]:
    - generic [ref=e3]: App
    - generic [ref=e4]:
        - img [ref=e6]
        - button "Open Tanstack query devtools" [ref=e54] [cursor=pointer]:
            - img [ref=e55]
```

# Test source

```ts
  1  | import { test, expect } from '@playwright/test';
  2  |
  3  | test.describe('Регистрация', () => {
  4  |   test('успешная регистрация нового пользователя', async ({ page }) => {
  5  |     const uniqueUsername = `user_${Date.now()}`;
  6  |     const uniqueEmail = `${uniqueUsername}@test.com`;
  7  |
  8  |     // Переходим на страницу регистрации
  9  |     await page.goto('/register');
  10 |
  11 |     // Заполняем форму
> 12 |     await page.getByLabel(/Имя пользователя/i).fill(uniqueUsername);
     |                                                ^ Error: locator.fill: Test timeout of 30000ms exceeded.
  13 |     await page.getByLabel(/Email/i).fill(uniqueEmail);
  14 |     await page.getByLabel(/Телефон/i).fill('+79991112233');
  15 |     await page.getByLabel(/Пароль/i).fill('password123');
  16 |
  17 |     // Сабмитим
  18 |     await page.getByRole('button', { name: /Создать аккаунт/i }).click();
  19 |
  20 |     // Если редирект не случился в течение 5 секунд, проверим, нет ли ошибки на экране
  21 |     try {
  22 |       await expect(page).toHaveURL(/\/login/, { timeout: 5000 });
  23 |     } catch (e) {
  24 |       const errorText = await page.locator('[class*="serverError"]').textContent();
  25 |       console.error('Registration failed with error:', errorText);
  26 |       throw e;
  27 |     }
  28 |   });
  29 |
  30 |   test('показ ошибки при пустых полях', async ({ page }) => {
  31 |     await page.goto('/register');
  32 |     await page.getByRole('button', { name: /Создать аккаунт/i }).click();
  33 |
  34 |     // Проверяем наличие сообщений о валидации
  35 |     await expect(page.getByText(/Минимум 3 символа/i)).toBeVisible();
  36 |     await expect(page.getByText(/Некорректный email/i)).toBeVisible();
  37 |   });
  38 | });
  39 |
```
